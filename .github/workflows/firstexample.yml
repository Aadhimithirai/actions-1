name: My First Worklflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo "My first github page"
        
        - name: List files
          run: ls

        - name: Read file
          run: cat README.md